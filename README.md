# WrapPointer-Logger
    WrapPointer (Wrapper class for pointer)
    Log Example)
      new = { A = { make = { B } } }
      new = { C = { make = { D } } }
      assign = { B = C }
      assign = { D = A }
      delete = { A = { delete = { B } }
      delete = { C = { delete = { D } }
      
